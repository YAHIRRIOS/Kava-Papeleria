<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/Direcciones.css">
    <link rel="stylesheet" href="/css/barra-navegacion.css">
    <title>Checkout</title>
</head>
<body>
    <%- include('template/barra-navegacion', { carrito }) %>

    <div class="container">
        <!-- Sección: Direcciones guardadas -->
        <div class="saved-addresses">
            <h3>Direcciones Guardadas</h3>
            <% if (direcciones && direcciones.length > 0) { %>
                <form action="/pago/pagoPaypal" method="POST">
                    <% direcciones.forEach((direccion, index) => { %>
                        <div class="address-card">
                            <div class="address-top-row">
                                <div class="radio-delete">
                                    <input type="radio" name="direccionSeleccionada" value="<%= direccion._id %>" <%= index === 0 ? 'checked' : '' %> />
                                    <button type="button" class="delete-btn" onclick="eliminarDireccion('<%= direccion._id %>')">🗑️ Eliminar</button>
                                </div>
                            </div>
                            <div class="address-details">
                                <p><strong>Calle:</strong> <%= direccion.calle %></p>
                                <p><strong>Ciudad:</strong> <%= direccion.ciudad %></p>
                                <p><strong>Código Postal:</strong> <%= direccion.codigoPostal %></p>
                                <p><strong>País:</strong> <%= direccion.pais %></p>
                            </div>
                        </div>
                    <% }) %>
                    <button class="proceed-btn" type="submit">Proceder al pago</button>
                </form>
            <% } else { %>
                <p>No tienes direcciones guardadas aún.</p>
            <% } %>
        </div>

        <!-- Formulario para agregar nueva dirección -->
        <div class="shipping-address">
            <h3>Agregar nueva dirección</h3>
            <% if (direcciones && direcciones.length >= 3) { %>
                <p style="color: red;">Solo puedes guardar hasta 3 direcciones.</p>
            <% } else { %>
                <form action="/pago/agregarDireccion" method="POST" id="form-direccion">
                    <div class="form-group">
                        <label>Calle</label>
                        <input type="text" name="calle" required>
                        <span id="calleError" style="color: red; display: none;">La calle debe tener al menos 3 caracteres válidos, contener letras y no incluir símbolos repetidos.</span>
                    </div>
                    <div class="form-group">
                        <label>Ciudad</label>
                        <input type="text" name="ciudad" required>
                        <span id="ciudadError" style="color: red; display: none;">La ciudad debe tener al menos 3 caracteres válidos, contener letras y no incluir símbolos repetidos.</span>
                    </div>
                    <div class="form-group">
                        <label>Código Postal</label>
                        <input type="text" name="codigoPostal" required>
                    </div>
                    <div class="form-group">
                        <label>País</label>
                        <select name="pais" required>
                            <option value="">Seleccione un país</option>
                            <option value="México">México</option>
                            <option value="Argentina">Argentina</option>
                            <option value="España">España</option>
                            <option value="Colombia">Colombia</option>
                            <option value="Chile">Chile</option>
                            <option value="Perú">Perú</option>
                            <option value="Estados Unidos">Estados Unidos</option>
                            <option value="Canadá">Canadá</option>
                        </select>
                    </div>
                    <button class="btn" type="submit">Guardar Dirección</button>
                </form>
            <% } %>
        </div>
    </div>

    <!-- Script de eliminación y validación -->
    <script>
        function eliminarDireccion(id) {
            fetch(`/pago/eliminarDireccion/${id}`, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' }
            })
            .then(response => {
                if (response.ok) {
                    location.reload();
                } else {
                    alert('Error al eliminar la dirección');
                }
            })
            .catch(error => {
                console.error('Error:', error);
                alert('Error al eliminar la dirección');
            });
        }

        document.addEventListener('DOMContentLoaded', function () {
            const form = document.getElementById('form-direccion');
            if (form) {
                form.addEventListener('submit', function (e) {
                    const calle = form.calle.value.trim();
                    const ciudad = form.ciudad.value.trim();
                    const codigoPostal = form.codigoPostal.value.trim();
                    const pais = form.pais.value;

                    const errores = [];

                    const regexValido = /^[a-zA-Z0-9\sáéíóúÁÉÍÓÚñÑ]+$/;
                    const contieneLetra = /[a-zA-ZáéíóúÁÉÍÓÚñÑ]/;
                    const secuenciasInvalidas = /[-\.]{3,}/;

                    // Validación calle
                    if (
                        calle.length < 3 ||
                        !regexValido.test(calle) ||
                        !contieneLetra.test(calle) ||
                        secuenciasInvalidas.test(calle)
                    ) {
                        errores.push('La calle debe tener al menos 3 caracteres válidos y contener letras.');
                        document.getElementById('calleError').style.display = 'block';
                    } else {
                        document.getElementById('calleError').style.display = 'none';
                    }

                    // Validación ciudad
                    if (
                        ciudad.length < 3 ||
                        !regexValido.test(ciudad) ||
                        !contieneLetra.test(ciudad) ||
                        secuenciasInvalidas.test(ciudad)
                    ) {
                        errores.push('La ciudad debe tener al menos 3 caracteres válidos y contener letras.');
                        document.getElementById('ciudadError').style.display = 'block';
                    } else {
                        document.getElementById('ciudadError').style.display = 'none';
                    }

                    // Código Postal
                    if (!/^\d{5}$/.test(codigoPostal)) {
                        errores.push('El código postal debe tener exactamente 5 dígitos numéricos.');
                    }

                    // País
                    if (pais === "") {
                        errores.push('Debe seleccionar un país válido.');
                    }

                    if (errores.length > 0) {
                        e.preventDefault();
                        alert(errores.join('\n'));
                    }
                });
            }
        });
    </script>
</body>
</html>


